# 构建上下文排除项。
#
# 口径：镜像要**自包含**——代码、Node 产物、六套索引、PDF/HTML 原文、模型权重全部进镜像，
# 换台机器/换系统只要 Docker 就能跑。所以这里不排除任何运行必需的知识库资产。
#
# 只有三类不进镜像：
#   1) 密钥     —— .env 绝不进镜像层
#   2) 本机运行期产物 —— logs/、个人 models/（llama.cpp 与 LLM 网关，属本机研究件）
#   3) 交付过程物 —— report/、projects/、历史回归归档（非运行时依赖）

# 密钥
.env
.env.*
!.env.example

# 版本控制与 Python 缓存
.git/
.gitignore
.github/
__pycache__/
**/__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.tmp/
.mypy_cache/
.ruff_cache/

# 本机运行期产物
logs/
.hf-cache/
models/

# 前端（独立构建上下文 web/RAG4ZRDDS，node_modules 不进后端上下文）
web/

# 交付与过程物（非运行时依赖）
report/
projects/
evaluation/runs/
evaluation/reports/runs/
